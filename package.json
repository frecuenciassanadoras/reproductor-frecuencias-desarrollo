{
  "name": "frecuencias-sanadoras",
  "version": "1.0.0",
  "description": "Netlify project for login and content access",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Building...' && netlify-lambda build netlify/functions",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.38.4",
    "netlify-lambda": "^2.0.16",
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.38"
  }
}
